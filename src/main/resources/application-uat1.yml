# =============================================================================
# UAT1 Environment Configuration
# =============================================================================
# User Acceptance Testing environment 1
# Run with: -Dspring.profiles.active=uat1
# Docker: docker-compose -f docker/docker-compose-uat1.yml up -d
# Ports: MySQL 3338, Redis 6409, Kafka 29122
# =============================================================================

server:
  port: 8081
  servlet:
    context-path: /api/v1/auth

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration

  # MySQL Database - UAT1 Server
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3338}/${DB_NAME:quckapp_auth_uat1}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:uat1_root_secret}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000

  # Redis - UAT1 Server
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6409}
      password: ${REDIS_PASSWORD:uat1_redis_secret}

  # Kafka - UAT1 Server
  kafka:
    bootstrap-servers: ${KAFKA_BROKERS:localhost:29122}

  # Flyway
  flyway:
    enabled: true
    validate-on-migrate: true
    baseline-on-migrate: false

# JWT Configuration - UAT1
jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 900000      # 15 minutes
  refresh-token-expiration: 604800000  # 7 days
  issuer: quckapp-auth-uat1

# OAuth2 Configuration
oauth2:
  frontend-callback-url: ${OAUTH2_FRONTEND_CALLBACK_URL:https://uat1.quckapp.com/auth/callback}
  mobile-deep-link-scheme: ${OAUTH2_MOBILE_SCHEME:quckapp-uat1://}

# Security Configuration
security:
  rate-limit:
    enabled: true
    login:
      max-attempts: 5
      window-seconds: 300
      block-duration-seconds: 900
  audit:
    enabled: true
    log-successful-auth: true
    log-failed-auth: true
  encryption:
    key: ${ENCRYPTION_KEY}

# Logging
logging:
  level:
    com.quckapp.auth: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
