# =============================================================================
# UAT2 Environment Configuration
# =============================================================================
# User Acceptance Testing environment 2
# Run with: -Dspring.profiles.active=uat2
# Docker: docker-compose -f docker/docker-compose-uat2.yml up -d
# Ports: MySQL 3348, Redis 6419, Kafka 29132
# =============================================================================

server:
  port: 8081
  servlet:
    context-path: /api/auth

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration

  # MySQL Database - UAT2 Server
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3348}/${DB_NAME:quckapp_auth_uat2}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:uat2_root_secret}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000

  # Redis - UAT2 Server
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6419}
      password: ${REDIS_PASSWORD:uat2_redis_secret}

  # Kafka - UAT2 Server
  kafka:
    bootstrap-servers: ${KAFKA_BROKERS:localhost:29132}

  # Flyway
  flyway:
    enabled: true
    validate-on-migrate: true
    baseline-on-migrate: false

# JWT Configuration - UAT2
jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 900000      # 15 minutes
  refresh-token-expiration: 604800000  # 7 days
  issuer: quckapp-auth-uat2

# OAuth2 Configuration
oauth2:
  frontend-callback-url: ${OAUTH2_FRONTEND_CALLBACK_URL:https://uat2.quckapp.com/auth/callback}
  mobile-deep-link-scheme: ${OAUTH2_MOBILE_SCHEME:quckapp-uat2://}

# Security Configuration
security:
  rate-limit:
    enabled: true
    login:
      max-attempts: 5
      window-seconds: 300
      block-duration-seconds: 900
  audit:
    enabled: true
    log-successful-auth: true
    log-failed-auth: true
  encryption:
    key: ${ENCRYPTION_KEY}

# Logging
logging:
  level:
    com.quckapp.auth: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
